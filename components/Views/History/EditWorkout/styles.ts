import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    addExerciseButtonWrapper: {
        alignItems: "flex-end",
        paddingHorizontal: 15,
        paddingVertical: 10,
        width: "100%"
    },
    addExerciseButton: {
        alignItems: "center",
        backgroundColor: "rgba(0, 0, 0, .7)",
        borderRadius: 25,
        justifyContent: "center",
        height: 50,
        width: 50
    }
});